<section class="relative min-h-screen flex items-end px-6 pt-36 pb-20" style="background: #0a0806;" data-dark-section>

  <!-- Clip container pour les orbs (overflow-hidden ici, pas sur la section) -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
    <!-- Grille perspective animée -->
    <div class="hero-grid"></div>
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>
    <!-- Grain dédié fond dark — overlay visible sur noir, contrairement au multiply global -->
    <div class="grain-hero"></div>
  </div>

  <div class="relative w-full max-w-6xl mx-auto">

    <!-- Top label row -->
    <div class="flex items-center gap-4 mb-12" data-animate="fade-up">
      <span class="section-label">Développeur Web Freelance · Alsace</span>
      <div class="h-px flex-1 max-w-[60px]" style="background: rgba(255,255,255,0.12);"></div>
      <span class="w-2 h-2 rounded-full" style="background: var(--accent-primary); box-shadow: 0 0 10px rgba(184,116,24,0.6);"></span>
      <span class="label-tech" style="color: rgba(255,255,255,0.45);">Disponible</span>
    </div>

    <!-- Main headline -->
    <div id="hero-headline" class="mb-10">
      <h1 class="font-['Syne'] leading-[0.92] tracking-tight" style="font-size: var(--type-display); font-weight: 800; color: #ffffff;">
        Pas une agence.<br />
        Pas un template.
      </h1>
      <div class="flex items-baseline gap-4 mt-3">
        <h1 class="font-['Syne'] leading-[0.92] tracking-tight italic hero-underline" style="font-size: var(--type-display); font-weight: 800; color: var(--accent-light);">
          Votre site.
        </h1>
      </div>
    </div>

    <!-- Bottom row: description + CTA + stats -->
    <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-10" data-animate="fade-up" data-animate-delay="180">

      <div class="max-w-md">
        <p class="text-lg leading-relaxed mb-8" style="color: rgba(255,255,255,0.58);">
          Un site professionnel qui vous ressemble, conçu pour convertir vos visiteurs en clients. Livré en 7 jours.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="#contact" class="btn-primary">Parlons de votre projet →</a>
          <a href="#realisations" class="btn-outline-dark">Voir mes réalisations</a>
        </div>
        <p class="mt-6 label-tech" style="color: rgba(255,255,255,0.35);">
          Disponible pour nouveaux projets · Délai garanti
        </p>
      </div>

      <!-- Stats -->
      <div class="flex gap-10 lg:gap-16 pb-2">
        <div>
          <p class="font-['Syne'] text-5xl leading-none" style="font-weight: 800; color: #ffffff;">7j</p>
          <p class="label-tech mt-2" style="color: rgba(255,255,255,0.4);">Délai moyen</p>
        </div>
        <div class="w-px" style="background: rgba(255,255,255,0.1);"></div>
        <div>
          <p class="font-['Syne'] text-5xl leading-none" style="font-weight: 800; color: #ffffff;">24h</p>
          <p class="label-tech mt-2" style="color: rgba(255,255,255,0.4);">Réponse garantie</p>
        </div>
        <div class="w-px hidden sm:block" style="background: rgba(255,255,255,0.1);"></div>
        <div class="hidden sm:block">
          <p class="font-['Syne'] text-5xl leading-none" style="font-weight: 800; color: #ffffff;">1:1</p>
          <p class="label-tech mt-2" style="color: rgba(255,255,255,0.4);">Interlocuteur direct</p>
        </div>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute right-0 bottom-0 hidden lg:flex flex-col items-center gap-3" data-animate="fade-up" data-animate-delay="300">
      <p class="label-tech" style="writing-mode: vertical-rl; color: rgba(255,255,255,0.3);">Découvrir</p>
      <div class="w-px h-12" style="background: linear-gradient(to bottom, rgba(255,255,255,0.2), transparent);"></div>
    </div>

  </div>
</section>

<style>
  /* ─── Grille perspective — plancher animé ─── */
  .hero-grid {
    position: absolute;
    bottom: -10%;
    left: 50%;
    width: 240%;
    height: 75%;
    transform: translateX(-50%) perspective(700px) rotateX(58deg);
    transform-origin: center bottom;
    background-image:
      linear-gradient(rgba(184, 116, 24, 0.18) 1px, transparent 1px),
      linear-gradient(90deg, rgba(184, 116, 24, 0.18) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: linear-gradient(to top, rgba(0,0,0,0.55) 0%, transparent 65%);
    -webkit-mask-image: linear-gradient(to top, rgba(0,0,0,0.55) 0%, transparent 65%);
    animation: grid-flow 5s linear infinite;
  }

  @keyframes grid-flow {
    from { background-position: 0 0; }
    to   { background-position: 0 80px; }
  }

  /* Orbs ambre — blob flou, animés via GSAP dans animations.ts */
  .orb {
    position: absolute;
    border-radius: 50%;
    pointer-events: none;
    will-change: transform;
  }

  .orb-1 {
    width: 800px;
    height: 800px;
    background: #b87418;
    filter: blur(150px);
    top: -200px;
    right: -100px;
    opacity: 0.55;
  }

  .orb-2 {
    width: 600px;
    height: 600px;
    background: #e8a44a;
    filter: blur(150px);
    top: 30%;
    left: -80px;
    opacity: 0.3;
  }

  .orb-3 {
    width: 500px;
    height: 500px;
    background: #7a4510;
    filter: blur(150px);
    bottom: 0;
    right: 25%;
    opacity: 0.4;
  }

  /* Grain dédié fond dark — overlay (visible sur noir, contrairement à multiply) */
  .grain-hero {
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    background-size: 180px;
    opacity: 0.07;
    mix-blend-mode: overlay;
    pointer-events: none;
  }

  /* Hero underline ambre */
  .hero-underline {
    position: relative;
    display: inline-block;
  }

  .hero-underline::after {
    content: '';
    position: absolute;
    bottom: -6px;
    left: 0;
    width: 100%;
    height: 3px;
    background: var(--gradient-cta);
    border-radius: 2px;
    opacity: 0.7;
  }

  /* btn-outline adapté fond dark */
  .btn-outline-dark {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 13px 28px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: rgba(255, 255, 255, 0.8);
    font-family: 'Syne', sans-serif;
    font-weight: 600;
    font-size: 0.9rem;
    letter-spacing: 0.04em;
    border-radius: 100px;
    cursor: pointer;
    transition: background 0.2s, border-color 0.2s, transform 0.2s;
    min-height: 44px;
  }

  .btn-outline-dark:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.4);
    transform: translateY(-2px);
  }
</style>
