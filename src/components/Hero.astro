<section class="relative min-h-screen flex items-end px-6 pt-36 pb-20 overflow-hidden">

  <!-- Ambient glows — chaleur sur fond clair -->
  <div class="absolute top-0 right-0 w-[700px] h-[700px] pointer-events-none" style="background: radial-gradient(ellipse at top right, rgba(232, 164, 74, 0.14) 0%, transparent 60%);"></div>
  <div class="absolute bottom-0 left-0 w-[500px] h-[500px] pointer-events-none" style="background: radial-gradient(ellipse at bottom left, rgba(232, 164, 74, 0.08) 0%, transparent 65%);"></div>

  <div class="relative w-full max-w-6xl mx-auto">

    <!-- Top label row -->
    <div class="flex items-center gap-4 mb-12" data-animate="fade-up">
      <span class="section-label">Développeur Web Freelance · Alsace</span>
      <div class="h-px flex-1 max-w-[60px]" style="background: var(--border-card);"></div>
      <span class="w-2 h-2 rounded-full" style="background: var(--accent-primary); box-shadow: 0 0 8px rgba(184,116,24,0.4);"></span>
      <span class="label-tech" style="color: var(--text-muted);">Disponible</span>
    </div>

    <!-- Main headline -->
    <div id="hero-headline" class="mb-10">
      <h1 class="font-['Syne'] leading-[0.92] tracking-tight" style="font-size: var(--type-display); font-weight: 800; color: var(--text-primary);">
        Pas une agence.<br />
        Pas un template.
      </h1>
      <div class="flex items-baseline gap-4 mt-3">
        <h1 class="font-['Syne'] leading-[0.92] tracking-tight italic hero-underline" style="font-size: var(--type-display); font-weight: 800; color: var(--accent-primary);">
          Votre site.
        </h1>
      </div>
    </div>

    <!-- Bottom row: description + CTA + stats -->
    <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-10" data-animate="fade-up" data-animate-delay="180">

      <div class="max-w-md">
        <p class="text-lg leading-relaxed mb-8" style="color: var(--text-secondary);">
          Un site professionnel qui vous ressemble, conçu pour convertir vos visiteurs en clients. Livré en 7 jours.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="#contact" class="btn-primary">Parlons de votre projet →</a>
          <a href="#realisations" class="btn-outline">Voir mes réalisations</a>
        </div>
        <p class="mt-6 label-tech">
          Disponible pour nouveaux projets · Délai garanti
        </p>
      </div>

      <!-- Stats -->
      <div class="flex gap-10 lg:gap-16 pb-2">
        <div>
          <p class="font-['Syne'] text-5xl leading-none" style="font-weight: 800; color: var(--text-primary);">7j</p>
          <p class="label-tech mt-2">Délai moyen</p>
        </div>
        <div class="w-px" style="background: var(--border-card);"></div>
        <div>
          <p class="font-['Syne'] text-5xl leading-none" style="font-weight: 800; color: var(--text-primary);">24h</p>
          <p class="label-tech mt-2">Réponse garantie</p>
        </div>
        <div class="w-px hidden sm:block" style="background: var(--border-card);"></div>
        <div class="hidden sm:block">
          <p class="font-['Syne'] text-5xl leading-none" style="font-weight: 800; color: var(--text-primary);">1:1</p>
          <p class="label-tech mt-2">Interlocuteur direct</p>
        </div>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute right-0 bottom-0 hidden lg:flex flex-col items-center gap-3" data-animate="fade-up" data-animate-delay="300">
      <p class="label-tech" style="writing-mode: vertical-rl;">Découvrir</p>
      <div class="w-px h-12" style="background: linear-gradient(to bottom, var(--border-card), transparent);"></div>
    </div>

  </div>
</section>

<style>
  .hero-underline {
    position: relative;
    display: inline-block;
  }

  .hero-underline::after {
    content: '';
    position: absolute;
    bottom: -6px;
    left: 0;
    width: 100%;
    height: 3px;
    background: var(--gradient-cta);
    border-radius: 2px;
    opacity: 0.6;
  }
</style>
